# NetSerial AI 运维终端 V0.9.1

<img src="design/app-icon-512.png" width="144" alt="NetSerial AI 应用图标">

NetSerial AI 是面向网络运维工程师的 Android 移动终端，整合 USB Console、SSH、Telnet、多厂商命令库、Tab/控制键、AI 命令草稿、变更管控、配置对比和现场网络工具。

开发人员：chenwei666

正式发布：[GitHub Release V0.9.1](https://github.com/chenwei666/NetSerial-AI/releases/tag/v0.9.1)（GitHub Latest）

正式 APK SHA-256：`8890591333b84658539bff9ba80a2367a85ca50825578c065d4d4c1e6884e646`

## V0.9.1 重点更新

- 连接 USB、SSH 或 Telnet 后，运维中心会显示当前活动终端，并可“一键读取当前设备并识别”。
- 读取内容来自现有终端最近输出，自动清理 ANSI、脱敏并限制为 100,000 字符，不创建第二条连接、不写入磁盘。
- 自动复用 H3C、Huawei、Cisco、Ruijie 指纹识别并切换命令/运维厂商上下文。
- 未连接或会话暂时无输出时显示可执行提示；不会为了识别而静默发送设备命令。

## V0.9.0 重点更新

- 完整应用内 AI 对话：多轮上下文、新建/切换/重命名/删除会话、停止、重试、复制与脱敏分享。
- AI 对话复用全部 18 类厂商和上游模型选择，支持活动档案优先、瞬时故障重试与安全故障转移。
- Android 6.0+ 使用 Android Keystore 加密保存有上限的对话历史；历史、设备记忆和终端上下文都在发送前脱敏。
- AI 回复中的围栏命令会经过白名单提取和本地 R0–R4 重新分级；R4 在 AI 页面保持阻断，其他命令也只能人工装载。
- 运维中心新增一键故障取证计划与 AI 深度分析；只读剧本增加提示符、超时、重试和首错即停执行合同。
- 配置 Diff 增加漂移等级和敏感变更计数；安全巡检增加 SSHv1、弱算法、SNMPv3、AAA 与集中日志检查。
- 变更任务启动前必须具备预检查、命令、验证和回退计划，完成前必须存在执行与验证证据。

## V0.8.0 重点更新

- 现代化任务卡片：主页、功能搜索、工具箱和设置统一采用 Material 3 卡片、清晰层级和更大的触控区域。
- AI 厂商扩展到 18 类入口：OpenAI、Anthropic、Gemini、DeepSeek、智谱 GLM、通义千问、豆包、腾讯混元/TokenHub、百度千帆、Kimi、MiniMax、硅基流动、Groq、Mistral、xAI、OpenRouter、自定义兼容接口和 HTTPS Ollama。
- 上游模型动态同步：按厂商调用模型目录 API，支持搜索选择、缓存、手动刷新、取消、离线缓存回退和手动模型名回退；密钥不进入模型缓存。
- 网络工具箱重做为快速诊断、结果、CIDR 计算和离线工具四个任务区，结果紧邻操作入口。
- 新增单目标最多 16 个 TCP 端口的受控检查、常用运维端口填充、地址属性摘要、结果复制/分享和并发任务保护。
- 保留 V0.7.0 的五区导航、AI 故障转移、高级诊断、签名运行手册、临时传输和受控批次能力。

## V0.7.0 重点更新

- 五区导航：主页、连接、终端、工具箱、设置。终端菜单只保留会话高频动作与必要上下文入口，低频全局功能不再重复堆叠。
- 全局功能搜索、最多 4 个收藏和最多 6 条最近使用；手机使用底部导航，平板自动切换侧边 Navigation Rail。
- AI 多厂商故障转移：当前厂商暂时失败时按配置顺序安全重试其他厂商，并显示最终厂商和尝试次数；API Key 继续由 Android Keystore 加密。
- 高级诊断：四厂商健康检查、CPU/内存/温度/接口错误分析、IP/MAC/接口定位、LLDP/CDP 拓扑解析、脱敏配置备份和 SNMPv3 只读采集计划。
- 安全高级工具：可直接用于 SSH 登录的设备凭据别名库、随机令牌 HTTP/只读 TFTP 临时共享、RSA/SHA-256 签名运行手册验证。
- 高风险自动化基础：金丝雀优先、逐目标审批、检查/变更/验证/回退、失败即停和回退策略；不绕过既有 R0-R4 与变更任务门禁。
- Android 16 预测式返回手势兼容；运行手册签名验证兼容 Android 5.0–16。

### V0.7.0 菜单

| 一级入口 | 内容 |
| --- | --- |
| 主页 | 当前设备、收藏、最近使用、快捷入口 |
| 连接 | USB、SSH/Telnet、多会话 |
| 终端 | 当前 USB 会话或远程/会话入口 |
| 工具箱 | AI、命令库、网络工具、运维中心、配置与安全高级工具 |
| 设置 | 设备记忆、应用设置、AI 厂商设置 |

右上角搜索可按中英文名称与关键词查找全部已注册工作区；会话专用动作仍保留在终端上下文中。长按功能卡可加入或移出收藏。

安全边界：HTTP/TFTP 只绑定私有或回环地址并在 10 分钟或 3 次下载后关闭；SNMPv3 当前只生成不含密钥的受限只读计划；设备/AI 凭据库需要 Android 6.0+；AI、运行手册和批次结果都必须由授权工程师复核。

## V0.6.0 新增

- 自动识别 H3C Comware、Huawei VRP、Cisco IOS、Ruijie RGOS；高置信度结果自动切换命令页与 Web 向导。
- 一键 Web 开通向导：按厂商和平台生成 HTTPS/可选 HTTP、账号与密码命令；密码仅在本次发送中使用，预览和证据全部脱敏。
- GitHub Latest 更新检测：支持启动后每日检查和手动检查，只接受 GitHub HTTPS Release 地址。
- AI 一键故障诊断：将脱敏终端输出带入任意已配置 AI 厂商，生成原因、只读检查、修复草案、验证与回退；本地记忆继续按用户授权写入。
- 运维中心：只读健康检查、接口诊断、VLAN 审计、LLDP/CDP 发现、安全基线采集、配置合规初筛和金丝雀优先批次规划。
- 多会话工作区：保存非敏感 SSH/Telnet 连接档案并在 Android 独立任务中并行打开；不保存密码或私钥。
- 配置快照中心：应用私有目录内保存脱敏、规范化、带 SHA-256 的配置快照，并直接进入差异与回退草案。
- Tab 补全 2.0 与自定义命令包：前缀优先、上下文辅助、厂商/视图隔离；自定义命令拒绝密钥和多行脚本。
- USB XMODEM-128 文件发送：CRC/校验和协商、重试、超时、取消、进度与发送前 SHA-256；受 R3 目标与变更门禁保护。
- 保留 V0.5.0 的现代 Material 3、深色模式、四套主题、中英文切换、SSH/Telnet/SFTP 与全部安全能力。

## 核心能力

- USB 串口：驱动探测、波特率、HEX、换行、流控、控制线和后台接收。
- SSH/Telnet：主机密钥校验、密码/键盘交互、会话私钥、跳板机、Keepalive 和 SFTP；Telnet 默认关闭且每次提示明文风险。
- 交换机命令：H3C Comware、Huawei VRP、Cisco IOS、Ruijie RGOS，按设备、接口、VLAN、三层、路由、环路、聚合、安全、排障、保存分类。
- AI：可配置多个内置厂商或自定义 OpenAI-compatible API；API Key 由 Android Keystore 保护；AI 输出只作为待审核草稿，不自动发送。
- 操作安全：R3 二次确认、R4 输入 `EXECUTE`、生产设备变更任务门禁、目标设备防错和敏感内容脱敏。
- 运维工具：IPv4/IPv6 CIDR、DNS、Ping、Traceroute、TCP、路径 MTU、MAC/OUI、常用端口、配置 Diff 和回滚草稿。
- 中英文：设置内可跟随系统或固定简体中文/English。

## 快速使用

1. 在“设备档案”中确认设备名称、厂商、环境和管理地址。
2. 从首页选择 USB 设备，或打开 SSH/Telnet 远程终端。
3. 使用 Tab、方向键、Ctrl+C 和问号快捷键辅助交互。
4. 从“命令库”按厂商和分类搜索；点击只填入/复制，长按可收藏。
5. 在“AI 设置”添加任意支持的厂商或兼容 API，再让 AI 检查、补全、诊断或生成四阶段命令计划。
6. 在“运维中心”识别厂商、生成只读剧本、执行配置合规初筛或建立金丝雀批次计划。
7. 生产变更先建立变更任务，并在结束后导出脱敏 Markdown/PDF 证据。

## 构建

需要 JDK 17 与 Android SDK。Windows 推荐：

```powershell
.\scripts\build.ps1
```

已初始化生产签名的发布工作站：

```powershell
.\scripts\build.ps1 -Release
```

详细边界与验证见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)、[docs/SECURITY.md](docs/SECURITY.md)、[docs/TEST_REPORT.md](docs/TEST_REPORT.md) 和 [docs/RELEASE.md](docs/RELEASE.md)。

## 现场验收要求

自动化验证不能替代真实设备测试。正式用于生产前，请在授权环境验证目标 Android 版本、USB OTG 芯片、四类真实交换机、SSH 私钥/跳板机/SFTP、系统网络工具和自有 AI 账号。所有 AI、Diff 和回滚内容都必须由具备权限的工程师复核。
