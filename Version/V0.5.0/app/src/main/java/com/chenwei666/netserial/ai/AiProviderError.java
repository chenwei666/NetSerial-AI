package com.chenwei666.netserial.ai;

public enum AiProviderError {
    AUTHENTICATION,
    RATE_LIMIT,
    SERVER,
    HTTP,
    NETWORK,
    TIMEOUT,
    TLS,
    RESPONSE_TOO_LARGE,
    INVALID_RESPONSE,
    CANCELLED
}
