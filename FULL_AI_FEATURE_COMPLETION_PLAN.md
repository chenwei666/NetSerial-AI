# NetSerial AI：交换机运维串口终端完整功能补全方案

- 方案版本：V1.0
- 日期：2026-08-21
- 方案负责人：chenwei666
- 基线分析：[ANALYSIS_AND_COMPLETION_PLAN.md](ANALYSIS_AND_COMPLETION_PLAN.md)
- 当前阶段：产品、架构、安全和验收方案；尚未开始编码

## 1. 产品定位与总体结论

产品建议命名为 **NetSerial AI 运维终端**。它不是简单地给串口软件加一个聊天框，而是把 USB 串口终端、厂商命令补全、AI Copilot、可控记忆、命令安全检查、配置备份和审计日志整合成一个现场运维工具。

最核心的设计原则：

1. **TAB 补全必须离线可用。** 本地命令语法库和真实 TAB 字节发送是主链路，AI 只负责复杂意图和模糊补全。
2. **AI 永远不能绕过命令安全策略。** 模型输出先变成结构化命令计划，再经过本地语法、上下文、风险和权限检查，最后由用户确认。
3. **记忆由应用控制，不绑定某一个 AI 厂商。** 本地保存可迁移、可查看、可删除的结构化记忆；厂商会话 ID 只作为可选加速能力。
4. **长期 API Key 不应作为生产默认方案保存在手机上。** 个人 BYOK 模式可用 Android Keystore 加密；企业生产模式使用自建 AI 网关和短期令牌。
5. **串口和基本运维能力不依赖网络。** AI 断网、限流或余额不足时，终端、TAB、命令库、日志和配置备份继续工作。
6. **设备上下文必须明确。** AI 生成命令前要知道厂商、系统版本、当前 CLI 模式、设备名称、串口参数和最近输出；不确定时只允许提问或给建议，不允许执行。

## 2. 核心用户场景

### 2.1 一句话生成并检查命令

用户说或输入：

> 把 GE1/0/1 配置成 access 口，加入 VLAN 116，描述改成办公网。

应用处理流程：

```text
语音/文字输入
  → 识别当前设备与命令模式
  → 从本地厂商命令库检索对应版本语法
  → AI 生成结构化命令计划
  → 本地语法与参数校验
  → 风险分级和影响说明
  → 展示实际命令、前置检查、验证命令、回滚命令
  → 用户确认
  → 逐条发送并等待提示符
  → 自动执行只读验证
  → 生成执行报告和可选记忆
```

配置变更默认不能“一句话直接执行”。一句话可以直接完成生成、检查和预览，但真正发送配置命令必须由用户确认。

### 2.2 AI 检查用户手写命令

用户在输入框写到一半，点击“AI 检查”：

- 检查命令是否属于当前品牌和系统版本。
- 检查是否位于正确模式，例如用户视图、系统视图、接口视图。
- 标出缺失参数、错误接口名、VLAN 范围、IP/掩码错误。
- 识别命令是否会中断管理链路、关闭接口、删除 VLAN、修改 AAA 或重启设备。
- 给出修正建议、执行前检查、执行后验证和回滚命令。
- 引用本地命令资料的来源、版本和章节。

### 2.3 TAB 补全

用户输入 `dis int br` 或 `show run int`：

- 单击实体 `Tab` 键：向交换机发送 ASCII HT `0x09`，使用设备自身补全。
- 本地补全：在输入框上方实时显示离线候选词和灰色补全文本。
- 双击 Tab：显示当前模式下的全部候选及参数说明。
- 输入 `?`：支持发送真实问号，也可显示本地上下文帮助。
- AI 补全：只在用户点击 AI、候选冲突或自然语言输入时调用，不在每次按键时联网。

### 2.4 AI 诊断当前故障

用户选择最近一段终端输出并问：

> 为什么这个端口一直 up/down？下一步检查什么？

AI 只先生成只读排障计划，例如接口状态、光功率、错误包、日志、聚合状态、STP 状态等；用户确认后逐条采集。发现可能需要配置变更时，自动切换到变更审批流程。

### 2.5 记住个人习惯和设备事实

用户可以说：

- “记住：这条线接 H3C 默认用 9600 8N1。”
- “记住：我们现场 VLAN 116 是办公网。”
- “以后 H3C 优先用 display current-configuration。”
- “忘掉刚才这个设备的所有记录。”

应用保存的是带作用域、来源、时间和可信度的结构化记录，不保存整段聊天作为不可控黑盒记忆。

## 3. AI 功能全景

### 3.1 AI Copilot 功能

| 功能 | 说明 | 默认执行策略 |
|---|---|---|
| 自然语言生成命令 | 根据设备、模式和目标生成命令计划 | 仅预览 |
| 命令补全 | 补全用户未写完的命令或参数 | 不自动发送 |
| 命令纠错 | 检查语法、视图、参数和版本差异 | 不自动发送 |
| 命令解释 | 逐项解释命令作用和影响 | 只读 |
| 风险识别 | 标记断网、重启、删除、AAA、固件等风险 | 强制执行 |
| 排障助手 | 从输出生成分阶段只读检查计划 | 只读命令可一键确认 |
| 配置生成 | VLAN、接口、链路聚合、路由、ACL 等配置 | 必须确认 |
| 配置审查 | 检查配置冲突、遗漏和潜在风险 | 只读分析 |
| 配置差异解释 | 对比备份，解释新增、删除和影响 | 只读分析 |
| 回滚建议 | 为变更步骤生成对应撤销动作 | 必须人工审核 |
| 输出摘要 | 将大量 CLI 输出整理成结论和证据 | 只读分析 |
| 报告生成 | 生成脱敏的巡检、故障和变更报告 | 导出前预览 |
| 资料问答 | 基于导入的厂商手册回答，并给出来源 | 只读 |
| 多厂商翻译 | 将某厂商命令意图映射到另一厂商 | 仅建议 |
| 语音助手 | 按住说话，转为文本后走同一安全流程 | 不直接执行 |

### 3.2 首批 AI 供应商适配

建议首批支持：

1. OpenAI。
2. Anthropic Claude。
3. Google Gemini。
4. DeepSeek。
5. 通义千问/Qwen。
6. 月之暗面/Kimi。
7. 通用 OpenAI-compatible 自定义地址。
8. 可选本地模型适配器，用于完全离线的解释、摘要或基础补全。

供应商差异隐藏在 `AiProviderAdapter` 内部。上层只能看到统一能力，例如结构化输出、流式响应、上下文长度、工具调用、嵌入和图片输入，不能在 UI 或业务代码里到处判断厂商名称。

### 3.3 AI 连接模式

#### 模式 A：个人 BYOK

适用：个人使用、测试、无法部署服务器的现场。

- 用户在 App 内选择供应商、粘贴 Key、选择模型并测试连接。
- Key 输入框默认遮挡，不提供“显示完整 Key”。
- 只显示供应商、配置名称、Key 末四位和最后验证时间。
- 使用 Android Keystore 中生成的不可导出 AES 密钥，以 AES-GCM 加密真实 Key；密文保存在应用私有且禁止备份的目录。
- 可选要求指纹/面容认证后才能使用 Key。
- Key 不进入 Room、日志、崩溃报告、剪贴板历史、导出文件、AI 记忆或终端会话。
- 删除供应商配置时同时删除密文、别名和相关缓存。
- 支持费用上限、日调用上限、单次 Token 上限和仅 Wi-Fi 模式。

限制：手机进程被攻破时，攻击者仍可能在应用使用 Key 的时刻发起请求。Keystore 可以提高提取难度，但不能把长期 Key 变成绝对安全。

#### 模式 B：企业 AI 网关，推荐生产使用

适用：团队、生产设备、统一审计和成本管理。

```text
Android App
  → 用户/设备身份认证
  → 获取短期、限作用域访问令牌
  → 企业 AI Gateway
      ├─ 供应商密钥保管
      ├─ 模型路由与故障切换
      ├─ 脱敏和策略检查
      ├─ 速率、配额、成本和审计
      └─ OpenAI / Claude / Gemini / DeepSeek / 私有模型
```

- 长期厂商 Key 永不下发到手机。
- App 只持有短期、可撤销、限设备和限权限令牌。
- 网关允许按人员、项目、供应商、模型和日预算授权。
- 网关不能直接向串口发送命令；执行审批仍由 App 本地安全模块控制。
- 支持私有部署和企业代理，不强制将运维数据交给第三方云服务。

### 3.4 供应商设置界面

每个供应商配置包含：

- 配置名称和供应商类型。
- 官方端点或自定义 HTTPS Base URL。
- API Key/短期令牌的安全引用，不存明文。
- 默认模型、快速模型、深度分析模型、嵌入模型。
- 超时、重试、并发、单次 Token 上限和日预算。
- 是否允许发送终端上下文、设备标识和导入文档。
- 是否启用供应商侧会话保存；默认关闭并优先使用本地记忆。
- “测试连接”只发最小请求，结果仅显示成功、错误类型、延迟和请求 ID，不打印 Key。
- 自定义地址必须使用 HTTPS；Debug 构建可显式允许本机测试地址，Release 禁止明文 HTTP。

模型名称和能力不写死。应用维护可更新的 `ModelCapability`，并允许用户手动选择，避免供应商改名或下线模型后必须升级整个 App。

## 4. AI 命令生成与安全执行闭环

### 4.1 AI 只能输出结构化命令计划

模型输出不能直接作为串口字节。统一结构示例：

```text
CommandPlan
  intent
  assumptions[]
  detectedDevice
  requiredMode
  prechecks[]
  steps[]
    id
    command
    expectedMode
    riskLevel
    reason
    waitForPrompt
    timeoutMs
    expectedPatterns[]
  verifications[]
  rollbackSteps[]
  warnings[]
  sourceRefs[]
  confidence
```

供应商支持严格 JSON Schema 时使用严格结构化输出；不支持时由适配器解析，并在结构校验失败时拒绝进入执行流程。

### 4.2 本地五层检查

AI 计划依次经过：

1. **结构检查**：字段完整、类型正确、步骤数量和长度受限。
2. **语法检查**：命令是否存在于当前厂商/版本语法库，参数格式是否合法。
3. **上下文检查**：当前提示符、配置视图和设备身份是否匹配。
4. **风险检查**：命令是否涉及中断、删除、重启、权限、固件或管理链路。
5. **策略检查**：当前用户、会话和部署策略是否允许发送。

任何一层不通过，结果只能显示为“建议”，不能进入发送队列。

### 4.3 风险等级

| 等级 | 示例 | 默认行为 |
|---|---|---|
| R0 信息 | 命令解释、手册查询 | 直接显示 |
| R1 只读 | display/show 查询 | 一次确认后可批量执行 |
| R2 一般变更 | VLAN、描述、普通接口参数 | 展示差异并确认 |
| R3 高风险 | 管理 IP、上联口、路由、STP、ACL、AAA | 逐步确认并要求验证/回滚 |
| R4 极高风险 | reboot、reset、format、删除配置、固件升级、关闭管理链路 | 禁止 AI 自动执行，要求输入设备名或风险短语 |

命令库中维护确定性风险规则；AI 只可提高风险等级，不能把本地判定的高风险降级。

### 4.4 执行保护

- 发送前展示原始命令，不只展示自然语言摘要。
- 展示设备名称、厂商、当前模式和目标接口，防止操作错设备。
- 高风险操作必须重新读取最新提示符，不能使用几分钟前的上下文。
- 逐条发送、等待提示符、记录回显；超时或异常输出立即停止。
- 识别 `[Y/N]`、确认口令、分页提示时暂停给用户，不让 AI 猜测确认答案。
- 设备断线、App 进入后台过久或身份变化后，未执行计划自动失效。
- 不把离线时排队的 AI 配置命令在网络恢复后自动发送。
- 变更后自动执行用户审核过的只读验证；失败时只展示回滚建议，不自动回滚高风险操作。

## 5. AI 记忆系统

### 5.1 记忆类型

| 类型 | 示例 | 默认写入方式 |
|---|---|---|
| 用户偏好 | 喜欢先看 display brief、默认中文解释 | 用户确认或明确“记住” |
| 设备事实 | 设备品牌、型号、版本、提示符、串口参数 | 从设备输出提取后确认 |
| 现场事实 | VLAN 116=办公网、机房/机柜标签 | 用户确认 |
| 已验证命令 | 在指定型号和版本上执行成功的命令 | 会话结束后确认 |
| 故障经验 | 某错误输出对应的已确认原因和处理 | 用户确认 |
| 会话摘要 | 本次检查、变更、验证和结果 | 默认本地保存，可关闭 |
| 安全规则 | 禁止关闭某上联口、禁止自动保存配置 | 管理员或用户显式配置 |

### 5.2 不允许进入记忆的数据

- API Key、Bearer Token、私钥、密码、SNMP community。
- 终端中的密码输入段和认证交互。
- 未经确认的 AI 猜测。
- 整段原始会话的无限期自动复制。
- 用户标记为敏感或临时的数据。

### 5.3 记忆作用域

每条记忆必须至少包含：

```text
MemoryRecord
  id
  type
  scope: global | vendor | site | device | session
  subjectId
  content
  source
  trust: observed | ai_suggested | user_verified | admin_verified
  createdAt
  lastUsedAt
  expiresAt
  version
  sensitiveClass
```

不同设备、现场和厂商的记忆不能混用。例如 H3C 某型号验证过的命令不能自动变成所有 Comware 设备的事实。

### 5.4 写入、召回和遗忘

- 默认采用“记忆候选”机制：AI 提议要记住什么，用户点选确认。
- 用户可以说“记住这个”“不要记住”“忘记设备 A”“清除最近 7 天记忆”。
- 设置页提供完整记忆列表、来源、使用次数、编辑、导出和删除。
- 召回只返回当前任务最相关的少量记录，不能把整个记忆库塞进提示词。
- 记忆变更有审计记录，但删除后正文不可恢复。
- 支持保留期限和自动过期；设备会话事实建议默认 180 天，具体由用户配置。

### 5.5 本地记忆与厂商会话

- 本地数据库是唯一标准事实来源，保证更换 AI 供应商后记忆仍可用。
- 供应商的 Conversation、Thread 或 cached context 仅作为可选能力。
- 默认尽可能使用无状态请求或关闭供应商侧持久存储；若用户启用远端会话，界面必须说明供应商、范围和删除入口。
- 长会话使用本地滚动摘要、关键事实和检索结果，不依赖无限增长的聊天历史。

### 5.6 知识库与记忆的区别

- **记忆**：用户和设备的已确认事实。
- **知识库**：厂商手册、命令参考、内部规范和命令包。
- **会话日志**：真实串口收发证据。

三者分开存储、分开授权。AI 回答必须能标明使用的是“手册来源”“本地记忆”还是“当前终端输出”。

## 6. TAB 与命令补全系统

### 6.1 三层补全

1. **设备原生补全**
   - 发送真实 `0x09` TAB 字节。
   - 保留设备返回内容和光标控制序列。
   - 支持长按、双击和外接键盘 Tab。

2. **本地确定性补全**
   - 根据厂商、系统、版本、当前模式和已输入 Token 查询命令语法树。
   - 延迟目标：P95 小于 50ms，不联网、不消耗 Token。
   - 支持前缀、缩写、模糊搜索、历史排序和参数占位提示。

3. **AI 意图补全**
   - 用户输入自然语言或本地候选不足时主动触发。
   - AI 只能返回经过本地语法库验证的候选；无法验证时标记“未验证建议”。

### 6.2 模式识别

`PromptDetector` 综合以下证据：

- 设备 banner、系统版本和厂商特征。
- 当前提示符，例如 `>`、`#`、`[name]`、`(config)#`。
- 最近进入/退出视图的命令。
- 设备真实 `?` 或 Tab 返回内容。

提示符相似时不能只凭一个正则判断厂商。设备身份不确定时，本地补全只展示通用安全候选，不发送 AI 生成的配置命令。

### 6.3 厂商命令包

首批建议：

- 华为 VRP。
- H3C Comware。
- Cisco IOS / IOS XE。
- 锐捷 RGOS。
- 后续：Cisco NX-OS、Juniper Junos、Aruba、Dell OS10、MikroTik、ZTE。

每个命令包包含：

- 厂商、操作系统、版本范围和语言。
- 命令 Token 树、视图、参数类型、帮助文本和缩写规则。
- 风险等级、前置条件、验证命令和可选回滚模板。
- 来源、许可证、维护者、包版本、校验和和数字签名。
- 自动化测试用例和真实设备验证状态。

命令包可以独立升级，但必须经过签名校验；团队自定义包默认不上传云端。

### 6.4 本地补全界面

- 输入框内灰色 ghost text，按 Tab 接受。
- 上方候选栏显示命令、简短说明、模式和风险颜色。
- 长按候选查看完整参数和来源。
- `?` 键区分“发送到设备”和“本地帮助”。
- 允许关闭智能补全，只保留原生 Tab。
- 中文搜索命令，例如输入“查看接口简要”匹配对应只读命令。

## 7. 其他完整功能建议

### 7.1 串口与终端

- FTDI、PL2303、CP210x、CH340/341、CDC ACM 和自定义 VID/PID。
- 5–8 数据位、全部常用波特率和自定义波特率、Parity、Stop Bits、Flow Control。
- RTS/DTR、CTS/DSR/CD/RI、BREAK、收发计数和连接质量状态。
- ANSI/VT100/VT220 基础兼容、颜色、光标、清屏、擦除、退格和分页。
- 中文、UTF-8、GBK、ASCII 等字符集；编码错误可视化。
- 横屏、分屏、平板双栏、外接键盘、鼠标选择、深色模式和字体缩放。
- `Esc`、`Tab`、`Ctrl+A-Z`、方向键、Home/End、PageUp/PageDown、Delete、`?`、`|` 自定义快捷键栏。
- 断线检测、受控重连、后台通知、锁屏保持和 USB 权限恢复。

### 7.2 设备与连接档案

- 线缆、USB VID/PID、端口、串口参数和最后成功连接记录。
- 品牌、型号、系统版本、序列号、站点、机柜和备注。
- 从 banner 自动提取设备候选信息，用户确认后保存。
- 二维码扫描选择设备档案，不在二维码中保存密码或 API Key。
- 启发式波特率检测和乱码评分，明确显示不确定性。

### 7.3 命令库与宏

- 按厂商、设备、模式、任务、风险和收藏分类。
- 文本、HEX、多行、无换行、字符延迟、行延迟和重复。
- 变量表单：VLAN、接口、IP、掩码、描述、下一跳等。
- 发送前渲染预览，禁止未替换变量进入发送队列。
- 命令历史去重、搜索、固定、删除和敏感命令不留历史。
- 团队命令包导入导出、签名验证和版本回滚。

### 7.4 配置备份、比较与合规

- 一键执行用户审核过的配置显示命令并保存原始输出。
- 按设备、日期和配置哈希归档。
- 文本 Diff、结构化 Diff、AI 变更解释和风险摘要。
- 标记基线配置，检查当前配置与基线差异。
- 内部规范检查使用确定性规则；AI 负责解释，不直接判定合规结论。
- 备份导出前进行口令、密钥、community 和敏感地址脱敏预览。

### 7.5 巡检与故障剧本

- 只读巡检模板：设备信息、CPU、内存、温度、电源、风扇、接口、错误包、日志、邻居、STP、聚合、路由。
- 故障剧本：接口不通、频繁 up/down、环路、VLAN 不通、链路聚合异常、光模块告警、管理登录失败。
- 每个步骤包含目的、命令、预期结果、异常分支和停止条件。
- AI 可以选择下一步，但必须从允许的剧本和命令库中选择。
- 自动生成证据清单和脱敏报告。

### 7.6 会话与日志

- 收发方向、毫秒时间戳、命令边界、提示符、断线和错误事件。
- 显示环形缓冲与磁盘流式日志分离，避免无限 TextView 造成内存增长。
- 搜索、过滤、书签、注释、只看命令、只看设备回显。
- 会话摘要、执行计划、用户确认、验证结果和导出记录形成审计链。
- 保留期限、最大空间、自动清理和加密导出。
- TXT、JSON、HTML/PDF 报告；原始日志和 AI 摘要明确区分。

### 7.7 文件传输

- Raw、Line、XMODEM、XMODEM CRC、XMODEM 1K 发送。
- 补充 XMODEM 接收，评估 YMODEM/ZMODEM。
- 文件类型、大小、SHA-256、设备电量和中断风险检查。
- 固件或 BootROM 操作使用独立高风险流程，不能由聊天消息直接启动。

### 7.8 多会话与远程协议

- 多 USB 端口和多会话标签，独立参数、日志和 AI 上下文。
- 会话标题始终显示设备名和连接方式，避免发错设备。
- SSH 作为可选独立模块，强制主机密钥验证。
- Telnet 默认禁用并标记不安全，只能由管理员策略显式开启。
- 串口、SSH、Telnet 共用终端和命令安全模块，但连接实现彼此隔离。

### 7.9 团队与企业能力

- 本地个人模式无需账号。
- 可选团队账号、角色、设备组、命令包和审批策略。
- 团队只同步已批准的模板和脱敏知识，不自动同步原始会话。
- 审批人不能远程越过现场用户直接向串口发送命令。
- 可选工单/CMDB 导出；任何数据库或企业系统连接均需独立授权和最小权限设计。

### 7.10 可用性与辅助能力

- 中文/英文完整本地化，术语统一。
- 按住说话，不做常驻监听；语音识别结果必须先展示文本。
- 字号、对比度、色盲友好风险标记、触觉反馈。
- 首次使用的线缆、OTG、驱动和权限诊断向导。
- 离线帮助、快捷键说明和厂商模式说明。

## 8. 页面与交互设计

### 8.1 主导航

```text
设备
终端
AI 助手
命令库
会话/备份
设置
```

### 8.2 终端页

```text
┌ 设备名 | 厂商/版本 | 当前模式 | 连接状态 ┐
│                                            │
│               终端正文                     │
│                                            │
├ 宏按钮 / AI 建议 / 风险提示 ───────────────┤
│ > 输入命令或自然语言……              [语音] │
├ Esc Tab Ctrl ↑ ↓ ← → ? |  [AI检查] [发送] ┤
└ 日志 | 搜索 | 书签 | AI | 更多              ┘
```

### 8.3 AI 计划卡片

必须显示：

- 理解到的目标和假设。
- 当前设备、系统版本和模式。
- 原始命令列表。
- 每条命令的作用和风险。
- 前置检查、执行后验证和回滚建议。
- 使用的手册、命令包和记忆来源。
- “仅复制”“插入输入框”“逐条确认执行”按钮。

不提供模糊的单一“执行全部”按钮处理高风险计划。

### 8.4 AI 设置页

- 个人 BYOK / 企业网关切换。
- 供应商配置列表和连接状态。
- 模型、预算、网络、上下文和隐私策略。
- 数据发送预览和脱敏规则。
- 记忆列表、知识库、导出和彻底删除。

## 9. 推荐技术架构

### 9.1 深模块划分

```text
app-ui
  ├─ 设备、终端、AI、命令库、会话和设置
  └─ 只依赖各深模块的小接口

session-orchestrator
  ├─ 会话状态、设备上下文、收发队列
  ├─ 计划执行、超时、取消和证据收集
  └─ 对外只暴露连接、发送和执行计划接口

terminal-engine
  ├─ ANSI/VT 状态机
  ├─ 光标、屏幕、编码和特殊键
  └─ 环形屏幕缓冲

completion-engine
  ├─ Prompt/模式识别
  ├─ 命令语法树、历史和本地搜索
  ├─ 原生 Tab/问号处理
  └─ AI 候选的本地约束

ai-copilot
  ├─ 上下文构建、脱敏和知识检索
  ├─ 供应商路由、结构化输出和重试
  ├─ 命令计划、解释、诊断和摘要
  └─ 不接触串口发送实现

provider-adapters
  ├─ OpenAI / Claude / Gemini / DeepSeek / Qwen / Kimi
  ├─ OpenAI-compatible
  ├─ Enterprise Gateway
  └─ Local Model

command-knowledge
  ├─ 厂商命令包、版本和来源
  ├─ 文本检索、可选向量检索
  └─ 签名、更新和回滚

memory-vault
  ├─ 结构化记忆、作用域和可信度
  ├─ 候选写入、召回、过期和遗忘
  └─ 与供应商无关的本地标准事实

execution-guard
  ├─ 确定性语法、风险和策略规则
  ├─ 用户确认与高风险锁
  └─ AI 不可绕过

secure-storage
  ├─ Android Keystore 与密文
  ├─ 数据库、日志和备份规则
  ├─ 脱敏和安全导出
  └─ 密钥生命周期

serial-adapters / remote-adapters
  ├─ USB Serial
  ├─ SSH（可选）
  └─ Telnet（默认禁用）
```

### 9.2 关键接口

```text
AiCopilot.analyze(AiRequest) -> CommandProposal
CompletionEngine.complete(CompletionContext) -> CompletionResult
ExecutionGuard.evaluate(CommandPlan, SessionContext) -> GuardDecision
MemoryVault.recall(MemoryQuery) -> MemoryBundle
MemoryVault.propose(MemoryCandidate) -> PendingMemory
SessionOrchestrator.execute(ApprovedPlan) -> ExecutionReport
CommandKnowledge.search(KnowledgeQuery) -> SourceBundle
CredentialVault.withCredential(ref, operation) -> SafeResult
```

每个模块把复杂实现藏在小接口后。供应商、串口芯片、命令包和存储都有生产适配器和测试适配器，测试通过同一个接口验证可观察结果。

### 9.3 推荐技术栈

- Kotlin、Coroutines、Flow、ViewModel。
- UI 可使用 Compose；高频终端正文使用独立自定义 View/渲染层。
- Room 保存结构化设备、命令、会话索引和记忆；大日志使用分块文件。
- DataStore 保存非敏感设置。
- Android Keystore + AES-GCM 保存 BYOK 密文。
- OkHttp 统一 TLS、超时、代理、流式响应和可取消请求。
- `usb-serial-for-android` 负责串口驱动。
- 本地检索先使用 SQLite FTS；向量检索作为可选能力，避免第一版复杂化。
- Gradle Version Catalog、依赖锁定、SBOM、R8 和可复现 Release 构建。

## 10. 核心数据模型

```text
AiProviderProfile
  id, providerType, displayName, baseUrl
  credentialRef, defaultModel, fastModel, reasoningModel
  capabilities, timeout, tokenLimit, dailyBudget
  privacyPolicy, enabled

DeviceProfile
  id, name, vendor, osFamily, osVersion, model, serialNumber
  site, rack, usbVid, usbPid, port
  baudRate, dataBits, parity, stopBits, flowControl
  charset, newline, terminalType, promptPatterns

SessionContext
  sessionId, deviceSnapshot, connectionType
  promptState, currentMode, recentTranscript
  activePlan, userPolicy, redactionPolicy

CommandGrammar
  packageId, vendor, osFamily, versionRange
  mode, tokens, parameters, abbreviations
  help, risk, preconditions, verification, sourceRefs

CommandPlan / CommandStep
  intent, assumptions, steps, prechecks
  verifications, rollback, warnings, sources, confidence

MemoryRecord
  id, type, scope, subjectId, content
  source, trust, timestamps, expiry, version, sensitivity

KnowledgeDocument
  id, title, vendor, osFamily, versionRange
  sourceUri, checksum, license, importedAt, chunks

ExecutionReport
  planSnapshot, approvals, commands, outputs
  startedAt, endedAt, result, failures, verification
```

所有历史对象保存快照。后续修改设备档案、命令包或 AI 模型时，不能改写过去的执行证据。

## 11. 隐私、安全与数据治理

### 11.1 发给 AI 前的上下文控制

- 默认只发送当前任务必要的最近输出，不发送整份配置。
- 永久剔除 API Key、Token、私钥、密码、community 和认证输入。
- 主机名、IP、VLAN、接口描述和序列号按企业策略选择保留、掩码或替换。
- 提供“本次将发送给 AI 的内容”预览。
- 原始串口日志与脱敏后的 AI 上下文分开保存。
- 第三方供应商的数据保留策略可能不同，供应商配置页必须显示链接和用户选择。

### 11.2 密钥与凭据

- APK、源码、资源、日志、备份、崩溃报告和测试数据中不得出现真实 Key。
- Key 页面启用安全窗口，阻止系统截图和最近任务缩略图。
- 密钥密文、记忆数据库和敏感日志排除云备份与设备迁移。
- Release 构建关闭网络正文日志；错误仅保留分类、状态码、请求 ID 和时间。
- 企业网关令牌使用短期、限作用域和可撤销设计。
- 支持密钥轮换、立即停用、最后使用时间和异常费用提醒。

### 11.3 AI 安全

- 终端输出、设备 banner 和导入文档全部视为不可信输入，防范提示词注入。
- 文档中的“忽略规则并执行命令”等文本不能改变系统策略。
- AI 无权直接调用串口、文件删除、系统 Shell 或后台自动执行工具。
- 模型拒绝、截断、结构错误或上下文超长时必须安全失败。
- 用户看见的解释不能代替原始命令和真实设备输出。

### 11.4 审计

- 记录谁、何时、在哪台设备上提出什么目标。
- 保存 AI 供应商、模型、提示模板版本、命令包版本和策略版本，不保存 Key。
- 记录 AI 建议、用户修改、确认、实际发送、设备回显和验证结果。
- 审计日志防篡改可采用分段哈希链；导出前仍进行脱敏。

## 12. 性能、稳定性与离线能力

- 串口接收、终端解析、日志写入和 UI 渲染使用独立受控队列。
- UI 不处理无限文本；屏幕缓冲、搜索索引和原始日志分层。
- TAB 本地补全 P95 小于 50ms。
- 普通输入不能因 AI 请求阻塞；AI 请求支持取消、超时和重试退避。
- AI 流式输出只更新计划预览，不边生成边执行。
- 网络切换、供应商限流、余额不足和模型下线都有明确错误分类和替代模型策略。
- 离线时保留串口、终端、原生 Tab、本地补全、命令库、日志、备份和只读剧本。
- 本地模型是可选下载包，有设备能力检测、存储提示和卸载入口。

## 13. 测试与 AI 评测方案

### 13.1 单元测试

- ANSI/VT 状态机、换行和字符集。
- Prompt/模式识别、命令 Tokenizer 和语法树。
- IP、掩码、VLAN、接口和范围参数校验。
- 风险规则、策略决策和确认状态机。
- 脱敏规则，保证密钥和认证输入不会进入提示词或日志。
- 记忆作用域、可信度、过期、删除和检索。
- Key 加密、轮换、删除和备份排除。

### 13.2 供应商契约测试

- 所有 AI 适配器使用本地 Mock Server 测试，不在 CI 使用真实 Key。
- 流式、超时、限流、无余额、错误 Key、模型下线和结构错误。
- 供应商返回自由文本时不能越过结构校验。
- 自定义 Base URL 的 HTTPS、重定向和证书错误处理。

### 13.3 AI 命令评测集

按厂商和系统版本建立脱敏评测集：

- 只读查询。
- 普通配置。
- 模式错误。
- 缺失参数和模糊目标。
- 管理链路中断。
- 删除、重启、格式化、AAA 和固件高风险命令。
- 输出中的提示词注入和恶意文本。
- 不存在的命令、跨厂商命令和版本不支持命令。

发布门槛建议：

- 本地危险命令规则评测集必须 100% 标记 R3/R4。
- AI 生成的每条可执行命令必须通过本地语法或明确进入“未验证建议”。
- 配置命令零自动发送；R4 零自动执行。
- 所有 Key、密码和 Token 脱敏用例零泄漏。
- TAB 本地补全 P95 小于 50ms。

### 13.4 硬件与设备测试

- Android 8、10、12、14、16 真机。
- FTDI、CH340/341、CP210x、PL2303、CDC ACM。
- 华为、H3C、Cisco、锐捷实际型号和版本。
- 9600/115200、自定义波特率、各类校验/停止位/流控。
- 拒绝权限、拔线、重插、旋转、锁屏、后台、低电量、存储不足。
- 10MB 以上日志、长稳、高速连续收发和 AI 同时工作。

## 14. 版本路线图

### V0.1.0：可信开源基线

- 独立包名、生产签名、完整中文/英文。
- 串口基线、权限、前台服务和可复现构建。
- 移除 Billing 和无关数据传输组件。
- 测试、Lint、依赖扫描、SBOM 和版本归档。

### V0.2.0：网络终端与真实 TAB

- ANSI/VT、自定义终端 View、环形缓冲。
- 实体 Tab、Ctrl、方向键、问号和外接键盘。
- 设备档案、日志和配置备份基础。

### V0.3.0：离线命令补全

- H3C、华为优先的命令语法包和模式识别。
- 本地候选、中文搜索、历史和风险标签。
- Cisco、锐捷命令包框架。

### V0.4.0：AI Copilot 与多供应商

- OpenAI、DeepSeek、Gemini 和 OpenAI-compatible 首批适配。
- 个人 BYOK、Keystore、连接测试、预算和隐私预览。
- 自然语言生成、命令检查、解释和结构化命令计划。
- 本地五层检查和风险确认。

### V0.5.0：记忆与知识库

- 结构化本地记忆、候选确认、作用域和遗忘。
- 厂商文档导入、FTS 检索、来源引用。
- 会话摘要和已验证命令沉淀。

### V0.6.0：自动化与诊断

- 只读巡检和故障剧本。
- `Send/Wait/Delay/Confirm/Capture/Assert/Stop` 有限步骤引擎。
- 执行后验证、回滚建议和报告。

### V0.7.0：企业模式

- 企业 AI 网关、短期令牌、角色、预算和集中策略。
- 签名命令包、团队模板和审批审计。
- 可选工单/CMDB 导出接口。

### V0.8.0：多会话和文件传输

- 多 USB 端口、多会话。
- XMODEM 接收和文件传输增强。
- 可选 SSH 模块。

### V1.0.0：生产验收

- 品牌、型号、Android 和串口芯片矩阵通过。
- AI 安全评测、长稳、异常恢复和渗透测试通过。
- README、隐私说明、部署、升级、回滚、运维和测试文档齐全。
- 完整 `Version/V1.0.0` 不可变归档、CHANGELOG、SBOM、签名和下载哈希验证。

## 15. 推荐的第一版范围

为了尽快满足最需要的两点，首个可用内测版建议合并 V0.1.0–V0.4.0 的最小集合：

1. H3C、华为真实 Tab 和本地命令补全。
2. OpenAI、DeepSeek、通用 OpenAI-compatible 三种 AI 连接。
3. 自然语言生成、AI 检查、命令解释和风险分级。
4. 结构化计划、人工确认、逐条发送和只读验证。
5. 本地设备档案、会话日志和基础记忆。
6. Key 的 Keystore 加密、禁止备份、日志脱敏和预算上限。

Cisco、锐捷、完整知识库、企业网关、多会话和 SSH 可在后续版本补齐，但架构从第一版起保留适配 seam，避免返工。

## 16. 关键产品决策建议

| 决策 | 推荐默认值 |
|---|---|
| AI 密钥模式 | 个人版支持 BYOK，企业版强制 Gateway |
| AI 供应商首批 | OpenAI、DeepSeek、OpenAI-compatible；随后 Gemini/Claude/Qwen |
| 首批厂商命令包 | H3C、华为；随后 Cisco、锐捷 |
| TAB | 原生 Tab + 本地语法补全，AI 仅主动触发 |
| AI 执行 | 默认只预览；只读可一次确认；配置逐步确认；R4 禁止自动执行 |
| 记忆 | 本地、结构化、确认后写入、可查看/导出/删除 |
| 云端记忆 | 默认关闭 |
| 日志 | 默认本地私有、受限保留、导出前脱敏 |
| 语音 | 按住说话、先转文字、不得直接执行 |
| 离线能力 | 终端、Tab、命令库、日志和备份必须可用 |
| minSdk | 默认 Android 8/API 26；如必须支持旧机再评估 API 21 |

## 17. 主要风险与控制措施

| 风险 | 控制措施 |
|---|---|
| AI 幻觉错误命令 | 本地命令包验证、来源、结构化计划、未验证标记 |
| 操作错设备 | 设备身份横幅、提示符复核、高风险输入设备名 |
| Key 被提取或滥用 | 企业网关优先、Keystore、预算、轮换、无日志 |
| 终端内容泄露给 AI | 最小上下文、永久脱敏、发送预览、供应商策略 |
| 提示词注入 | 设备输出和文档均视为不可信，策略模块不可被模型修改 |
| 自动化失控 | 有限步骤、停止条件、逐条回显、配置不自动执行 |
| 错误记忆污染 | 候选确认、来源和可信度、作用域、过期和删除 |
| 厂商版本差异 | 命令包版本范围、设备版本检测、真实设备验证 |
| 长会话卡顿 | 专用终端 View、环形缓冲、分块日志和后台索引 |
| 升级破坏历史 | 数据迁移测试、模型/命令包快照、版本不可变归档 |

## 18. 开发启动前需要确认

进入编码前需要确认以下业务选择：

1. 首批交换机优先顺序，建议 H3C、华为、Cisco、锐捷。
2. 实际型号、系统版本、常用串口线芯片和 Android 设备。
3. 第一版 AI 供应商，建议 OpenAI、DeepSeek、OpenAI-compatible。
4. 个人 BYOK 是否足够，还是同时部署企业 AI Gateway。
5. 哪些只读命令允许一次确认批量执行。
6. 哪些配置命令必须永久禁止 AI 执行。
7. 日志、会话和记忆的保留期限及脱敏规则。
8. 内部使用还是公开发布，以及隐私政策和许可证要求。

在用户明确选择真实供应商并进入开发阶段前，不检查、创建或保存任何真实 API Key。

## 19. 参考资料

- OpenAI API 身份认证和 Key 安全：https://platform.openai.com/docs/api-reference/authentication
- OpenAI Responses 和结构化输出：https://platform.openai.com/docs/api-reference/responses
- OpenAI 数据控制：https://platform.openai.com/docs/models/default-usage-policies-by-endpoint
- Android Keystore：https://developer.android.com/privacy-and-security/keystore
- Android 密码学建议：https://developer.android.com/privacy-and-security/cryptography
- Android 自动备份与排除规则：https://developer.android.com/identity/data/autobackup
- Gemini API Key 安全：https://ai.google.dev/gemini-api/docs/api-key
- SimpleUsbTerminal：https://github.com/kai-morich/SimpleUsbTerminal
- usb-serial-for-android：https://github.com/mik3y/usb-serial-for-android

本方案中的命令包和 AI 执行策略必须以用户实际设备、厂商文档和真机验收为准。任何模型输出都不应被视为设备厂商的正式配置建议。
